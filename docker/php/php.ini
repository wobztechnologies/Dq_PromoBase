; Custom PHP configuration for production

; Memory
memory_limit = 512M

; Upload
upload_max_filesize = 100M
post_max_size = 100M
max_file_uploads = 20

; Execution
max_execution_time = 300
max_input_time = 300
default_socket_timeout = 300

; Error handling - show errors in logs for debugging
display_errors = Off
display_startup_errors = Off
log_errors = On
error_log = /dev/stderr
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT

; Session
session.gc_maxlifetime = 7200
session.cookie_httponly = On
; Let Laravel handle secure cookie setting based on HTTPS detection
; session.cookie_secure will be set by Laravel based on request scheme
session.use_strict_mode = On

; Security
expose_php = Off
allow_url_fopen = On
allow_url_include = Off

; Date
date.timezone = Europe/Paris

; Realpath cache
realpath_cache_size = 4096K
realpath_cache_ttl = 600
